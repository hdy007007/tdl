<%= render 'shared/content_title', title: '编辑' %>

<div class="well">
  <%= form_for @todo, :html => {class: 'form-horizontal'} do |f| %>
    <div class="form-group">
      <%= f.label :标题, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
      <%= f.text_field :title, class: 'form-control', autofocus: true %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :内容, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
      <%= f.text_area :description, class: 'form-control' %>
      </div>
    </div>

    <div class="form-group">
        <%= f.label :标签, class: 'col-sm-2 control-label' %>
      <div class="checkbox col-sm-10">
      <%= f.collection_check_boxes :category_ids, current_user.categories.all, :id, :name do |cb| %>
        <% cb.label(class: "checkbox-inline"){cb.check_box(class: "checkbox-inline") + cb.text} %>
      <% end %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :期限, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
      <%= f.datetime_select :deadline, class: 'form-control', :ampm => true, :minute_step => 5, :include_blank => true %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit(@todo.new_record? ? "Add ToDo" : "完成", class: 'btn btn-default') %>
        <%= link_to '取消', root_path, class: 'btn btn-default' %>

      </div>
    </div>
  <% end %>
</div>
